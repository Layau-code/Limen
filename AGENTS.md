# Limen 开发协作规范

## 项目定位

Limen 是使用 Go 实现、兼容 OpenAI API 协议的 AI Gateway。它位于 AI 应用与模型 Provider 之间，为内部 AI 应用和 Agent 提供统一、可靠、可观测的模型访问入口。

本项目同时用于系统实践 Go 后端与 AI 基础设施工程。所有设计都应真实、易解释、可测试；不为展示技术栈而增加复杂度。

## 目标用户

- 公司内部 AI 应用与 Agent 服务的开发者。
- 需要统一管理模型访问的后端平台团队。
- 本项目不面向普通终端用户提供聊天产品。

## 当前阶段：MVP

MVP 只完成一条可验证的最小链路：

```text
Client / Agent
  -> OpenAI-Compatible API
  -> API Key Authentication
  -> OpenAI Provider
  -> Streaming / Non-streaming Response
  -> Logs
```

MVP 包含：

- `POST /v1/chat/completions`。
- OpenAI Provider。
- 流式与非流式转发。
- 基础 API Key 鉴权。
- 请求取消、超时和资源释放。
- 结构化日志、单元测试和必要的集成测试。

MVP 不包含：

- 其他 Provider 和复杂 Provider 抽象。
- 多模型动态路由、Retry、Fallback、熔断。
- RPM、TPM 和分布式限流。
- Usage 持久化、成本统计、计费。
- 管理后台、数据库、Redis、消息队列。
- 完整 OpenTelemetry、Prometheus 和 Grafana 体系。

这些能力只有在后续阶段需求明确后才引入。

## 工程原则

1. 遵循 Go 习惯，不照搬 Java 分层和设计模式。
2. 保持最小依赖；标准库可以清楚解决时，不引入框架。
3. 接口由真实替换或测试需求驱动，不预先抽象未来 Provider。
4. `context.Context` 沿调用链传递；客户端断开时必须取消上游请求。
5. 流式响应不得无界缓冲；必须正确关闭响应体并避免 goroutine 泄漏。
6. 错误应保留原因并在 API 边界统一映射，不记录密钥或完整敏感请求。
7. 新行为必须有测试；修复缺陷前先添加可复现测试。
8. 优先保证代码能运行、能测试、能理解、能解释、能分析性能。

## 代码与注释

- 注释保持简短，只解释设计原因、业务约束、非显然行为和重要边界，不复述代码本身。
- 导出的 Go 标识符按需提供符合 GoDoc 习惯的注释；命名已经清楚的局部代码不添加无效注释。
- 修改功能时优先复用、替换或整理现有实现，避免通过不断追加分支、文件和重复逻辑来堆叠功能。
- 若新增代码使原有模块职责变乱，应在同一次改动中完成必要的小范围重构；不保留已经失效的旧实现。
- 删除重复和不可达代码，保持函数与文件职责单一；不为不确定的未来需求预留抽象。

## 测试代码

- 测试聚焦可观察行为和关键边界，不重复验证语言或标准库行为。
- 优先使用表驱动测试和少量清晰的测试辅助函数，避免重复搭建、过度 mock 和庞大 fixture。
- 每个测试应能直接看出场景、输入和预期结果；只有确实提升可读性时才引入额外抽象。
- 测试代码与生产代码遵循相同的简洁标准，不以覆盖率为理由堆积低价值用例。

## 文档同步规则

代码和文档属于同一个交付物。以下变化必须在同一次改动中更新对应文档：

- 所有文档正文统一使用简体中文；代码、命令、API、环境变量和必要的技术专有名词保留原文。

- 用户可见行为、配置、启动方式或 API 变化：更新 `README.md`。
- 项目范围、架构、数据流或关键设计决策变化：更新 `docs/design.md`。
- 开发流程、目录约束、测试要求或协作规则变化：更新 `AGENTS.md`。

提交前必须检查文档是否仍与代码一致。若无需更新文档，应能说明原因。

## 开发约束

- 提交保持小而聚焦，不夹带无关重构。
- 配置来自环境变量或配置文件，密钥不得写入代码、日志或仓库。
- 对外 API 优先兼容 OpenAI 协议；有意偏离时必须记录原因。
- 新增依赖、后台服务或基础设施前，先说明其解决的当前问题。
- 完成改动前运行格式化、静态检查和测试；具体命令随项目脚手架确定后写入本文件和 `README.md`。

## 完成标准

一次改动只有同时满足以下条件才算完成：

- 行为符合当前 MVP 范围。
- 正常路径、关键错误路径和取消路径有验证。
- 没有明显的资源泄漏、竞态或敏感信息暴露。
- 相关文档已同步。
- 格式化、静态检查和测试通过。
