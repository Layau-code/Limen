# Chat 错误响应安全边界

## 问题

Chat 解析器对未知消息角色的错误消息直接拼接客户端输入。即使没有泄露密钥，这也会让用户输入进入公共错误、日志或上层 SDK，违反稳定错误和隐私边界。

## 方案

- 保留固定的 `unsupported message role` 错误，不回显角色值。
- 继续使用 `unsupported_field` 处理工具、结构化输出和未知字段拒绝。
- 保持错误响应的 OpenAI envelope 和稳定错误码不变。

## 验收

- 带敏感标记的未知角色不会出现在解析错误中。
- 已有兼容矩阵和未知字段拒绝测试继续通过。

